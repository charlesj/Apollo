---
- name: stop services
  become: true
  systemd:
    name: "{{ item }}"
    state: stopped
  with_items:
    - apollo-client
    - apollo-server
- include_tasks: build_and_copy.yml
- include_tasks: db_migrations.yml
- include_tasks: services.yml
