---
- hosts: apollo-host
  vars:
    dbname: apollo_db
    dbuser: apollo_pg
    dbpassword: apollo_db_password
    dblisten_addresses:
      - localhost
  roles:
    - base
    - dotnet
    - node
    - database
    - haproxy
    - publish
